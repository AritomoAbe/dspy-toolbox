[pytest]
log_cli = true
log_cli_level = DEBUG
addopts = --cov=proc --cov-fail-under=95 --cov-report=term-missing -m "not llm"
markers =
    llm: integration tests requiring a running LLM instance (deselect with '-m "not llm"')
